# LRU-Cache-Library
Java LRU cache library using HashMap + doubly linked list  for strict O(1) operations. Supports optional thread safety, TTL expiration,  and includes a lightweight performance benchmark.
